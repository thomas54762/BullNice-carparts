# Root-level environment files
.env
.envrc
.env.local
.env.*.local

# OS-specific files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# Editor directories and files (root level)
.vscode/
.idea/
*.swp
*.swo
*~
.project
.classpath
.settings/

# Logs (root level)
*.log
logs/

# Temporary files
*.tmp
*.temp
.cache/

# Build artifacts (root level, if any)
dist/
build/
*.egg-info/

# Coverage reports (root level)
.coverage
htmlcov/
.coverage.*
coverage.xml
*.cover

# Python virtual environments (root level)
.venv/
venv/
env/
ENV/
env.bak/
venv.bak/

# Node modules (root level, if any)
node_modules/

# Lock files (root level, if any monorepo tooling)
package-lock.json
yarn.lock
pnpm-lock.yaml

# IDE specific
*.sublime-project
*.sublime-workspace
*.code-workspace

