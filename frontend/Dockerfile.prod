FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .
RUN npm run build

# The build output will be in /app/dist
# This will be shared via Docker volume to nginx container
CMD ["echo", "Frontend build complete"]

